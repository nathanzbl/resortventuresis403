<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Resort Properties Ventures - Landing Page</title>

    <style>
        :root {
            /* Color Variables*/
            --color-light-bg: #EAE3D3;
            --color-dark-bg: #985E47;
            --color-heading: #985E47;
            --color-button: #738F73;
            --color-text-light: white;
            --font-primary: 'Georgia', serif;
        }

        /* Basic Reset and Body Setup */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: Arial, sans-serif;
            background-color: #eee;
            /* Added padding for fixed navigation bar */
            padding-top: 60px; 
        }
        
        /* --- NAVIGATION BAR STYLES --- */
        .top-nav {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 60px;
            background-color: var(--color-dark-bg);
            color: var(--color-text-light);
            display: flex;
            justify-content: space-between; 
            align-items: center;
            padding: 0 40px;
            z-index: 100;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.2);
        }

        /* Nav Branding/Logo */
        .nav-brand {
            color: var(--color-text-light);
            text-decoration: none;
            font-size: 1.5rem;
            font-weight: bold;
            font-family: var(--font-primary);
        }

        .nav-links {
            display: flex;
            align-items: center;
            height: 100%;
        }

        /* Styling for Home and Login links */
        .nav-links a {
            color: var(--color-text-light);
            text-decoration: none;
            padding: 10px 15px;
            margin-left: 20px;
            font-size: 1rem;
            transition: background-color 0.2s;
            border-radius: 5px;
        }

        /* Styling for the dropdown toggle buttons */
        .dropdown-toggle {
            color: var(--color-text-light);
            text-decoration: none;
            padding: 10px 15px;
            margin-left: 20px;
            font-size: 1rem;
            transition: background-color 0.2s;
            border-radius: 5px;
            cursor: pointer;
            display: flex; 
            align-items: center;
            position: relative; 
        }

        /* Dropdown Arrow Indicator */
        .dropdown-toggle::after {
            content: '\25BC';
            font-size: 0.6rem;
            margin-left: 8px;
            transition: transform 0.3s;
        }

        .nav-links a:hover,
        .dropdown-toggle:hover {
            background-color: rgba(255, 255, 255, 0.1);
        }
        
        /* Login Button Styling (NAV BAR) */
        .nav-links .login-btn {
            background-color: var(--color-button);
            padding: 8px 20px;
            border-radius: 5px;
            font-weight: bold;
        }

        .nav-links .login-btn:hover {
             background-color: #617c61;
        }
        
        /* Dropdown open state styles */
        .dropdown:hover .dropdown-toggle::after {
            transform: rotate(180deg);
        }

        .dropdown {
            position: relative;
            display: inline-block;
            height: 100%; 
            display: flex; 
            align-items: center;
        }

        .dropdown-content {
            display: none; /* Hidden by default */
            position: absolute;
            top: 100%; 
            right: 0; 
            background-color: var(--color-dark-bg);
            min-width: 220px; 
            box-shadow: 0px 8px 16px 0px rgba(0,0,0,0.2);
            z-index: 101;
            border-radius: 0 0 5px 5px;
            padding: 5px 0;
            text-align: left;
        }

        .dropdown:hover .dropdown-content {
            display: block; /* Show on hover */
        }

        .dropdown-content a {
            color: var(--color-text-light);
            padding: 10px 16px;
            text-decoration: none;
            display: block;
            font-size: 0.95rem;
            margin: 0;
            border-radius: 0;
        }

        .dropdown-content a:hover {
            background-color: #83553f; 
        }
        /* --- END NAVIGATION BAR STYLES --- */
        
        /* --- MAIN 4-QUADRANT LAYOUT STYLES --- */
        .main-page-grid {
            display: grid;
            grid-template-columns: 1fr 1fr; /* Two equal columns */
            width: 100vw;
            min-height: 80vh; 
            overflow: hidden;
        }

        .content-left {
            display: grid;
            grid-template-rows: 1.2fr 1fr;
        }

        .content-right {
            display: grid;
            grid-template-rows: 1fr 1fr;
        }

        /* Header/Info Section (Top Left) */
        .header-section {
            background-color: var(--color-light-bg);
            padding: 50px;
            padding-bottom: 30px; 
            display: flex;
            flex-direction: column;
            justify-content: center;
        }

        .header-section h1 {
            font-family: var(--font-primary);
            color: var(--color-heading);
            font-size: 3.2rem;
            line-height: 1;
            margin-bottom: 5px;
        }

        .venture-text {
            font-size: 1.2rem;
            color: var(--color-heading);
            margin-bottom: 25px;
            font-weight: normal; 
        }

        .header-section hr {
            border: none;
            border-top: 1px solid rgba(152, 94, 71, 0.5); 
            width: 60%;
            margin-bottom: 30px;
            margin-left: 0;
        }
        
        /* --- HOME PAGE LOGIN BUTTON STYLES --- */
        .home-login-button {
            background-color: var(--color-button);
            color: var(--color-text-light);
            border: none;
            padding: 12px 35px;
            border-radius: 5px;
            font-size: 1rem;
            cursor: pointer;
            transition: background-color 0.3s;
            align-self: flex-start;
            margin-top: 10px;
            text-decoration: none; 
            text-align: center;
            display: inline-block;
        }

        .home-login-button:hover {
            background-color: #617c61;
        }
        /* --- END HOME PAGE LOGIN BUTTON STYLES --- */

        /* Welcome Section (Bottom Left) */
        .welcome-section {
            background-color: var(--color-dark-bg);
            padding: 50px;
            color: var(--color-text-light);
            display: flex;
            flex-direction: column;
            justify-content: center;
        }

        .welcome-section h2 {
            font-family: var(--font-primary);
            font-size: 2.2rem;
            font-weight: 700;
            line-height: 1.1;
            margin-bottom: 20px;
        }

        .welcome-section p {
            font-size: 1rem;
            line-height: 1.6;
            max-width: 95%;
        }

        /* Image Areas (Right Side) */
        .image-top, .image-bottom {
            background-size: cover;
            background-position: center;
            min-height: 40vh; 
        }
        
        /* Will replace images later */
        .image-top {
            /* Placeholder Image 1 */
            background-image: url('https://picsum.photos/900/450?random=1');
        }

        .image-bottom {
            /* Placeholder Image 2 */
            background-image: url('https://picsum.photos/900/450?random=2');
        }

        /* --- SITE FEATURES SECTION STYLES (3x2 Grid) --- */
        .features-section {
            background-color: var(--color-light-bg);
            padding: 50px 40px;
            color: var(--color-heading);
            text-align: left;
        }

        .features-section h3 {
            font-family: var(--font-primary);
            font-size: 2rem;
            margin-bottom: 20px;
        }

        .feature-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr); /* 3 columns for desktop (3x2 format) */
            gap: 20px;
            margin-top: 20px;
        }

        .feature-card {
            background-color: white;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            transition: transform 0.2s;
            border-top: 4px solid var(--color-button);
            min-height: 150px; 
        }

        .feature-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 6px 12px rgba(0, 0, 0, 0.15);
        }

        .feature-card h4 {
            font-family: var(--font-primary);
            color: var(--color-dark-bg);
            margin-bottom: 10px;
        }

        .feature-card p {
            font-size: 0.9rem;
            line-height: 1.5;
        }

        .feature-card a {
            color: var(--color-dark-bg);
            font-weight: bold;
            text-decoration: underline;
        }
        
        /* --- RESPONSIVE ADJUSTMENTS --- */
        @media (max-width: 768px) {
            body {
                padding-top: 50px; /* Smaller padding for mobile nav */
            }

            .top-nav {
                height: 50px;
                padding: 0 10px;
            }

            .nav-brand {
                font-size: 1.2rem;
            }
            
            .nav-links a, .dropdown-toggle {
                margin-left: 5px;
                padding: 5px 8px;
                font-size: 0.8rem;
            }

            .dropdown-content {
                min-width: 180px;
            }
            
            .main-page-grid {
                /* Collapse to single column stack on mobile */
                grid-template-columns: 1fr;
                grid-template-rows: auto;
                min-height: auto;
            }

            .content-left {
                grid-template-rows: auto auto; 
            }
            
            .content-right {
                grid-template-rows: auto auto; 
            }

            .feature-grid {
                grid-template-columns: 1fr; /* Single column on mobile */
            }

            .header-section, .welcome-section, .features-section {
                padding: 30px 20px;
            }

            .header-section h1 {
                font-size: 2.5rem;
            }

            .welcome-section h2 {
                font-size: 1.8rem;
            }
        }
    </style>

</head>
<body>

    <!-- Fixed Navigation Bar -->
    <nav class="top-nav">
        <a href="#" class="nav-brand">Resort Properties</a>
        
        <div class="nav-links">
            <a href="#">Home</a>
            
            <div class="dropdown">
                <a href="#" class="dropdown-toggle">Properties</a>
                <div class="dropdown-content">
                    <a href="#">Hawaii</a>
                    <a href="#">Mexico</a>
                    <a href="#">Mammoth</a>
                    <a href="#">Tahoe</a>
                </div>
            </div>
            
            <div class="dropdown">
                <a href="#" class="dropdown-toggle">Tools</a>
                <div class="dropdown-content">
                    <a href="#">Schedules</a> 
                    <a href="#">Directory</a>
                    <a href="#">Exchange Form</a>
                    <a href="#">Change Your Info</a>
                    <a href="#">Feedback</a> 
                </div>
            </div>
            
            <a href="#" class="login-btn">Login</a>
        </div>
    </nav>

    <!-- MAIN LANDING PAGE CONTENT -->
    <div id="mainPage">
        <div class="main-page-grid">
            <div class="content-left">
                <div class="header-section">
                    <h1>Resort Properties</h1>
                    <p class="venture-text">Ventures</p>
                    <hr>
                    <!-- Home page Login Button -->
                    <a href="#" class="home-login-button">Login</a>
                </div>
                <div class="welcome-section">
                    <h2>Welcome Shareholders</h2>
                    <p>Welcome to the web site for Resort Property Ventures shareholders. The goal of the site is to provide pertinent information and serve as a forum for shareholders and management to communicate.</p>
                </div>
            </div>

            <div class="content-right">
                <div class="image-top"></div>
                <div class="image-bottom"></div>
            </div>
        </div>

        <!-- SITE FEATURES SECTION: 3x2 GRID -->
        <div class="features-section">
            <h3>Key Site Features</h3>
            <div class="feature-grid">
                
                <!-- Card 1: Contact & Communication -->
                <div class="feature-card">
                    <h4>Shareholder Communication</h4>
                    <p>Connect with other partners directly from the <a href="#" data-page="addressBookPage">Directory</a>. You can email individuals or groups segmented by property.</p>
                </div>
                
                <!-- Card 2: Property Exchange -->
                <div class="feature-card">
                    <h4>List/Exchange Property</h4>
                    <p>Easily list your weeks for rent, trade, or sale by submitting the <a href="#" data-page="exchangeFormPage">Exchange Form</a>. Listings appear in the <a href="#" data-page="schedulesPage">Schedules</a> view.</p>
                </div>
                
                <!-- Card 3: Management Contacts -->
                <div class="feature-card">
                    <h4>Management Directory</h4>
                    <p>Find contact information for all board members and key management staff instantly on the <a href="#" data-page="addressBookPage">Directory</a> page.</p>
                </div>
                
                <!-- Card 4: View Schedules -->
                <div class="feature-card">
                    <h4>Access Schedules</h4>
                    <p>View upcoming shareholder meetings, property schedules, and download printer-friendly versions of the latest <a href="#" data-page="schedulesPage">Schedules</a>.</p>
                </div>
                
                <!-- Card 5: Raise Concerns -->
                <div class="feature-card">
                    <h4>Property Management Contact</h4>
                    <p>If you have specific concerns or comments regarding an individual property, please contact your dedicated property manager via the <a href="#" data-page="addressBookPage">Directory</a>.</p>
                </div>
                
                <!-- Card 6: Provide Feedback -->
                <div class="feature-card">
                    <h4>Provide Organizational Feedback</h4>
                    <p>We welcome suggestions for both the organization and the website. Submit your valuable input through the <a href="#" data-page="commentsPage">Feedback Form</a>.</p>
                </div>
                
            </div>
        </div>
    </div>

</body>
</html>
